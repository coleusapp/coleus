#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

git pull origin main && docker compose pull && docker compose up -d

docker compose exec app php artisan vendor:publish --tag=laravel-assets --force
docker compose exec app php artisan migrate --force
